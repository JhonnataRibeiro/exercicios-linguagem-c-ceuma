#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    int circu;
    float pi = 3.14;
    double result;

    printf("Informe a circuferencia: ");
    scanf("%d", &circu);

    result = pi * (pow(result, 2));
    printf("%f\n", result);
    return 0;
}
