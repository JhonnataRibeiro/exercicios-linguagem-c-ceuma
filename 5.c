#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    int valor; //criando uma variavel para guardar um valor do tipo inteiro
    valor = 50; //atribuindo o valor 50 na variavels

    printf("Valor da minha variavel: %d\n", valor);

    return 0;
}
